#include <stdio.h>
#include <stdlib.h>

int main(void)
{
  int result = system("ls -al");
  int c = getc(stdin);
  return 0;
}
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
  int result = system("ls -al");
  int c = getc(stdin);
  return 0;
}aa
